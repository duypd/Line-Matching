machine:
php:
version: 7.0.12
checkout:
  post:
    - git submodule sync
    - git submodule update --init
 
test:
  minitest_globs:
    - test/integration/**/*.rb
    - test/extra-dir/**/*.rb
dependencies:
  pre:
    - gem uninstall bundler
    - gem install bundler --pre
